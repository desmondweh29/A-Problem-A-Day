#include <bits/stdc++.h>
using namespace std;

// Question: https://codeforces.com/problemset/problem/1537/B

void solve()
{
    long long n, m, i, j;
    cin >> n >> m >> i >> j;

    long long x1, y1, x2, y2;

    cout << 1 << " " << 1 << " " << n << " " << m << "\n";
}

int main() 
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;

    while (t--)
    {
        solve();
    }
}